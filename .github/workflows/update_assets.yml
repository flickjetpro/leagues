name: Update Sports Assets

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  harvest_and_map:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install -r scripts/requirements.txt

      - name: 1. Fetch TSDB (Primary)
        run: python scripts/fetch_tsdb.py

      - name: 2. Fetch Streamed (Gap Filler)
        run: python scripts/fetch_streamed.py

      - name: 3. Generate Map
        run: python scripts/generate_map.py

      - name: Commit & Push Changes
        run: |
          git config --global user.name "AssetBot"
          git config --global user.email "bot@noreply.github.com"
          
          git add assets/logos/
          git add assets/data/
          
          if git diff --staged --quiet; then
            echo "No changes to assets."
          else
            git commit -m "Auto-Update: New Logos & Map [Skip CI]"
            git push
          fi
